apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: app
  labels:
    app: visits-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      initContainers:
        - name: wait-for-config-server
          image: curlimages/curl:8.9.1
          command:
            - sh
            - -c
            - |
              echo "Waiting for config-server..."
              until curl -sf -o /dev/null http://config-server:8888; do
                sleep 5
              done
              echo "config-server is up."
        - name: wait-for-discovery-server
          image: curlimages/curl:8.9.1
          command:
            - sh
            - -c
            - |
              echo "Waiting for discovery-server..."
              until curl -sf -o /dev/null http://discovery-server:8761; do
                sleep 5
              done
              echo "discovery-server is up."
      containers:
      - name: visits-service
        image: arupsscet1/spring-petclinic-visits-service:${IMAGE_TAG}
        ports:
        - containerPort: 8082
        resources:
          limits:
            memory: "512Mi"
          requests:
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 6


---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
spec:
  selector:
    app: visits-service
  ports:
    - port: 8082
      targetPort: 8082